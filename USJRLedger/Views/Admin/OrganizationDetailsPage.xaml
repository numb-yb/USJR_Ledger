<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="USJRLedger.Views.Admin.OrganizationDetailsPage"
             Title="Organization Details">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">
            <Frame BorderColor="Gray" Padding="15">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Organization Information" FontSize="20" FontAttributes="Bold" />

                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto">
                        <Label Grid.Row="0" Grid.Column="0" Text="Name:" FontAttributes="Bold" />
                        <Label Grid.Row="0" Grid.Column="1" x:Name="OrganizationNameLabel" />

                        <Label Grid.Row="1" Grid.Column="0" Text="Department:" FontAttributes="Bold" />
                        <Label Grid.Row="1" Grid.Column="1" x:Name="DepartmentLabel" />

                        <Label Grid.Row="2" Grid.Column="0" Text="Status:" FontAttributes="Bold" />
                        <Label Grid.Row="2" Grid.Column="1" x:Name="StatusLabel" />

                        <Label Grid.Row="3" Grid.Column="0" Text="Created Date:" FontAttributes="Bold" />
                        <Label Grid.Row="3" Grid.Column="1" x:Name="CreatedDateLabel" />

                        <Label Grid.Row="4" Grid.Column="0" Text="Deactivation Date:" FontAttributes="Bold" />
                        <Label Grid.Row="4" Grid.Column="1" x:Name="DeactivationDateLabel" IsVisible="False" />

                        <Label Grid.Row="5" Grid.Column="0" Text="Current Balance:" FontAttributes="Bold" />
                        <Label Grid.Row="5" Grid.Column="1" x:Name="BalanceLabel" FontAttributes="Bold" />
                    </Grid>

                    <Button x:Name="UpdateStatusButton" 
                            Clicked="OnUpdateStatusClicked"
                            Margin="0,10,0,0" />
                </VerticalStackLayout>
            </Frame>

            <Frame BorderColor="Gray" Padding="15">
                <VerticalStackLayout Spacing="10">
                    <Grid ColumnDefinitions="*,Auto">
                        <Label Grid.Column="0" Text="Advisers" FontSize="20" FontAttributes="Bold" />
                        <StackLayout Grid.Column="1" Orientation="Horizontal" Spacing="5">
                            <Label Text="Total:" VerticalOptions="Center" />
                            <Label x:Name="AdvisersCountLabel" VerticalOptions="Center" FontAttributes="Bold" />
                        </StackLayout>
                    </Grid>

                    <ListView x:Name="AdvisersListView" HeightRequest="200">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <Grid Padding="10" ColumnDefinitions="*,Auto">
                                        <VerticalStackLayout Grid.Column="0">
                                            <Label Text="{Binding Name}" FontAttributes="Bold" />
                                            <Label Text="{Binding Username}" FontSize="12" TextColor="Gray" />
                                        </VerticalStackLayout>
                                    </Grid>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>